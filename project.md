# 无人机扫描任务闭环管理系统｜项目说明 V2.3.1

> 本文档随项目实际实现同步更新。V2.2 在 V2.1 基础上完成“扫描任务化”改造：
> 系统更名（复扫 → 扫描）、新增任务类型（复扫 / 通扫）与扫描次数统计、
> 列表按计划扫描日期按周分组、状态流新增“需再次扫描”、导出台账扩展为 15 列。
> V2.2.1：导出拆分为「按周周报」（周可选，默认本周；精简 8 列 + 美化样式，直发工作群）
> 与「全部台账备份」（15 列全字段、表头首行不变，导入兼容）两种。
> V2.3：新增「司机」字段（表单、详情、导出同步）；连续录入改为保留任务类型、线路、
> 飞手、司机与计划日期（杆塔/区段不再保留）；任务列表同周同线路合并为可展开的母行；
> 导出台账扩列（全部台账 15→16 列、按周周报 8→9 列），导入兼容有/无司机列的文件。
> V2.3.1：「计划扫描日期」改名「开始时间」、「实际扫描日期」改名「结束时间」（仅中文展示文案，
> 英文字段名 planDate/actualDate 不变，旧数据 / 旧导出文件无缝兼容）；新建 / 编辑表单中
> 开始时间与结束时间同一行展示；逾期规则改为按结束时间判断（无结束时间按开始时间）。
> 旧 localStorage 数据无缝迁移，以本文为准。

---

## 1. 项目定位

电力线路无人机巡检后的“扫描任务闭环管理网页”。把扫描任务（含复扫、通扫两种类型）从产生到闭环的主流程跑通：

```text
发现问题 → 建立扫描任务（复扫 / 通扫）→ 派发责任人 → 现场扫描 → 填写结果 → （需再次扫描）→ 确认闭环 → 导出台账
```

第一版为**本地原型版**：纯前端，数据存在浏览器本地，单人/演示可用；后续可接数据库升级为多人协同。

---

## 2. 技术栈（实际使用）

```text
React 18 + Vite 5 + Tailwind CSS 3
react-router-dom        路由（HashRouter）
localStorage            第一版本地数据存储
exceljs                 Excel 台账导出 / 导入（动态加载，不进首屏）
```

视觉特效相关依赖：

```text
gsap                            发光卡片粒子/光影动画（MagicBento）
three                           背景流动线条（FloatingLines，懒加载）
@splinetool/react-spline +
@splinetool/runtime             工作台首页 3D 机器人（懒加载）
```

性能设计：5 个页面均按路由代码分割（React.lazy），three / gsap / react 拆分独立 vendor chunk，
exceljs 与 Spline 仅在使用时动态加载，首屏 JS 约 200KB（gzip 后更小）。

项目位置：`/Users/dayu/Documents/1、UOM/12、任务派发网页`
启动：`npm install` → `npm run dev`（默认 http://localhost:5173）。
注意：这是前端工程，**不能直接双击 index.html 打开**，必须通过本地服务器或部署后访问。

---

## 3. 页面结构

```text
1. 工作台首页      /
2. 扫描任务列表    /tasks（全部任务）、/tasks/rescan（复扫任务）、/tasks/general（通扫任务）
3. 新建 / 编辑任务 /tasks/new、/tasks/:id/edit
4. 任务详情        /tasks/:id
5. 数据导出        /export
```

左侧导航：工作台、**扫描任务**（父级菜单，下设 全部任务 / 复扫任务 / 通扫任务 三个子页面）、新建任务、数据导出。
顶部为系统标题栏，右上角“新建任务”按钮。

---

## 4. 任务数据结构（V2.2，已调整）

```js
{
  id: "uuid",
  taskNo: "FS-20260615-001",   // 自动生成：FS-年月日-三位序号
  taskType: "复扫",             // 任务类型：'复扫' | '通扫'（V2.2 新增，默认 '复扫'，旧数据自动迁移）
  lineName: "500kV 云海一线",   // 线路名称（必填）
  towerRange: "123#-126#",      // 杆塔 / 区段（必填）
  pilot: "张三",                // 责任飞手（可选）
  pilot2: "李明",               // 协助飞手（可选，第二位人员）
  driver: "王师傅",             // 司机（可选，V2.3 新增，默认 ''）
  planDate: "2026-06-15",       // 开始时间（必填）
  actualDate: "",               // 结束时间（可选）
  status: "待复扫",             // 当前状态（必填）
  scanCount: 0,                 // 扫描次数（V2.2 新增，默认 0，每次进入“已完成”自动 +1）
  rescanResult: "",             // 扫描结果（在“任务详情”页填写）
  conclusion: "",               // 闭环结论（在“任务详情”页填写）
  remark: "",                   // 备注（保留在数据结构，详情/导出可见）
  createdAt: "2026-06-15 10:00:00",
  updatedAt: "2026-06-15 10:00:00"
}
```

localStorage key 不变（`drone_rescan_tasks_v1`），英文字段名不变，旧数据打开即自动迁移（补 taskType / scanCount），无缝兼容。

### 与最初 V1.0 的差异（重要）

- **已删除字段**：问题类型（issueType）、问题描述（issueDescription）、责任站点（station）。
- **新增字段**：协助飞手（pilot2）—— 人员由 1 位扩展为 2 位（责任飞手 + 协助飞手，均可选）。
- **V2.2 新增字段**：任务类型（taskType，'复扫' | '通扫'）、扫描次数（scanCount，进入“已完成”自动 +1）。
- **V2.3 新增字段**：司机（driver，可选，默认 ''；旧 localStorage 数据自动迁移补 ''）。
- **扫描结果 / 闭环结论**：不在“新建/编辑”表单中，改为在“任务详情”页填写。
- **备注**：保留在数据结构中（详情页有内容时显示、导出包含），但已从新建表单移除。
- **V2.2 文案更名**：计划复扫日期→计划扫描日期、实际复扫日期→实际扫描日期、复扫结果→扫描结果（仅中文展示文案，英文字段名不变）。
- **V2.3.1 文案更名**：计划扫描日期→开始时间、实际扫描日期→结束时间（仅中文展示文案，英文字段名 planDate/actualDate 不变，旧数据与旧导出文件无缝兼容）。

---

## 5. 页面功能说明

### 5.1 工作台首页

- 顶部 **Hero 区**：左侧标题文案，右侧可交互 3D 机器人（鼠标在整页任意位置移动都会跟随），叠加聚光灯，外层液态玻璃卡片。
- 统计卡片（发光卡）共 7 张：扫描任务总数、待复扫、已派发、已完成、需再次扫描、已闭环、逾期未完成。支持键盘 Tab 聚焦、Enter/空格跳转。
- 最近扫描任务列表（最新 5 条）。

### 5.2 扫描任务列表（核心主台账）

分三个子页面：全部任务（/tasks）、复扫任务（/tasks/rescan）、通扫任务（/tasks/general），复扫 / 通扫子页面按 taskType 过滤。

表格字段：任务编号、类型（仅“全部任务”页显示）、线路名称、杆塔/区段、责任飞手、协助飞手、开始时间、扫描次数、当前状态、操作。

**按周分组**：列表按“开始时间”（planDate）所在周分组展示（周一起始），各周之间以周标题行分隔，标题含 ISO 8601 世界通用周序号（如 `W29 · 7月13日 ~ 7月19日 · 3 条`）；无开始时间的任务归入“未安排日期”组。

**同线路合并行**（V2.3 新增）：每个周组内，相同线路名称且多于 1 条的任务合并为一个「母行」——杆塔/区段列合并展示（如 100#-101#、120#-121#、150#-160#），编号列显示「共 N 条」；点击母行可展开/收起查看各子任务行（子行缩进显示，全部原有交互保留）。只有 1 条的线路保持普通行，空线路名不参与合并。

功能：新建、编辑、删除（玻璃风弹窗二次确认）、查看详情、行内改状态。

筛选 / 搜索：

```text
关键词搜索：线路名称 / 杆塔区段 / 责任飞手 / 协助飞手
下拉筛选：按线路、按状态（状态筛选与 URL 参数同步，刷新/分享不丢失）
```

**分页**：每页 20 条，底部翻页条（页码 + 上一页/下一页），筛选变化自动回到第 1 页。

可访问性：任务行支持 Tab 聚焦、Enter 进入详情。

### 5.3 新建 / 编辑任务

表单分两块：

```text
基础信息：任务类型*（复扫任务 / 通扫任务）、线路名称*、杆塔 / 区段*
人员与时间：责任飞手、协助飞手、司机、开始时间*、结束时间、当前状态*
（* 为必填；开始时间与结束时间同一行展示）
```

- 任务编号自动生成 `FS-年月日-三位序号`（同日序号自增）。
- **连续录入优化**（V2.3 调整）：点“创建任务”后不跳转，保留任务类型、线路、飞手（责任 / 协助）、司机与开始时间共 6 项；**杆塔/区段不再保留、创建后清空**（因不同任务杆塔号不同），结束时间清空、状态重置为“待复扫”不变（防止误带）；自动换下一条编号、按钮变“继续创建”，顶部提示「已保留任务类型、线路、飞手、司机与开始时间」并提供“查看该任务/查看列表”；底部“清空表单”可一键重填。

### 5.4 任务详情

- 状态流转步骤条 + 一键快速改状态，带业务约束（弹窗确认）：
  - 回退状态（如已完成 → 待复扫）需二次确认；
  - 标记“已完成”时未填扫描结果会提示确认；
  - 标记“已闭环”时未填闭环结论会提示确认。
- 基础信息：编号、类型、线路、杆塔/区段、责任飞手、协助飞手、司机、开始/结束时间、扫描次数、备注、创建/更新时间。
- **扫描结果 / 闭环结论**：在本页填写并保存。
- 进入“已完成”且无结束时间时，自动补全结束时间为当天；每次进入“已完成”，扫描次数自动 +1。
- 删除任务使用玻璃风确认弹窗（ConfirmModal）。

### 5.5 数据导出 / 导入

导出拆分为两张卡片（上下排列，均为 exceljs 动态加载）：

- **导出周报（周可选，直接发工作群）**：下拉选择任意有任务的周（默认本周，保证本周始终在列表中），筛出开始时间在所选周（周一起始）的任务，精简 9 列：任务编号、线路名称、杆塔/区段、责任飞手、协助飞手、司机、开始时间、当前状态、扫描结果。
  - 美化样式：标题行 + 周序号副标题、状态着色、逾期标红、隔行填充、底部统计行。
  - 工作表名 `W周数周报`，文件名 `无人机扫描周报_W周数_日期范围.xlsx`。
  - 下拉项显示周序号 / 日期范围 / 任务条数并标注「（本周）」；所选周无任务时按钮禁用。
- **导出全部台账（数据备份）**：保留 16 列全字段（任务编号、任务类型、线路名称、杆塔/区段、责任飞手、协助飞手、司机、开始时间、结束时间、扫描次数、当前状态、扫描结果、闭环结论、备注、创建时间、更新时间），表头仍在第 1 行，保证导入兼容；工作表名「扫描任务台账」与文件名 `无人机扫描任务台账_当前日期.xlsx` 均不变。
  - 美化样式：品牌蓝表头、状态着色、逾期标红、隔行填充、冻结首行。
- 概览卡新增「本周计划」统计（开始时间落在本周的任务条数）。
- **导入 Excel 台账**：功能不变，选择 `.xlsx` 文件按表头中文名解析，兼容旧版 13 列、15 列与新版 16 列文件（无任务类型 / 扫描次数 / 司机列时按默认值迁移，司机列缺失时默认空 ''）；日期列表头新旧双名兼容——旧文件的「计划扫描日期」「实际扫描日期」与新表的「开始时间」「结束时间」均可正确解析；
  - 合并导入：按任务编号去重，跳过已存在编号；
  - 替换导入：覆盖本机全部数据（需弹窗二次确认）。
  - 导入结果内联显示（成功 N 条 / 跳过 M 条）。
- 导出/导入结果与错误均为页面内联提示，不再使用浏览器原生弹窗。
- 底部提供“恢复内置示例数据”（弹窗二次确认）。

---

## 6. 状态流转、颜色与逾期

状态流转（V2.2 新增“需再次扫描”，共五态）：

```text
待复扫 → 已派发 → 已完成 → 需再次扫描 → 已闭环
```

原则：**已完成 ≠ 已闭环**。飞手完成扫描只代表现场任务完成，管理员确认结果有效后才算闭环；结果不达标可置为“需再次扫描”，再次完成后进入闭环。

状态配色（V2.0：去紫，统一蓝白银；V2.2 新增琥珀色）：

| 状态 | 颜色 |
|---|---|
| 待复扫 | 蓝 |
| 已派发 | 青蓝 |
| 已完成 | 绿 |
| 需再次扫描 | 琥珀（文字 #f5b85c） |
| 已闭环 | 银灰 |
| 逾期未完成 | 红色提示 |

逾期规则：

```text
当前日期 > 结束时间（无结束时间时按开始时间）且 当前状态不是“已闭环”
```

---

## 7. 视觉风格（V2.0 已大改）

最初 V1.0 要求的“正式、干净、不要科技风/动画/3D”等约束**已废弃**，现采用深色科技风 + 液态玻璃：

- **整体配色**：深蓝底 + 蓝 / 白 / 银，主色蓝 `#3884ff`（无紫色）。
- **背景**：FloatingLines 流动线条（three.js，蓝白银渐变；桌面端懒加载，页面切后台自动暂停；移动端降级为静态渐变背景）。
- **全局鼠标光影**：蓝色光晕跟随光标在整个页面移动，并点亮附近卡片（rAF 节流）。
- **发光统计卡**：MagicBento 风格（粒子 / 边缘发光 / 点击涟漪）。
- **液态玻璃（极致版）**：面板与主按钮采用强霜面玻璃（高斯模糊 + 镜面高光斜切 + 亮边 + 投影）；工作台 Hero 启用 SVG 位移扭曲，背景线条透过玻璃液态折射。
- **3D 机器人**：工作台首页 Spline 机器人，全页面鼠标跟随。
- **返回按钮**：圆形玻璃图标按钮（统一组件）。
- **确认弹窗**：统一的玻璃风 ConfirmModal，替代浏览器原生 confirm/alert。

---

## 8. 数据存储与部署

### 8.1 现状（localStorage）

- 数据存在**访问者本人浏览器**，单网站约 5MB 上限，可存几千~上万条文字记录，够日常使用。
- 写入带异常兜底（超出配额等失败时返回 false 并输出错误日志，不会静默丢操作）。
- 局限：不跨设备/浏览器共享、不能多人协同、清缓存会丢、无备份。
- 建议定期用“数据导出”存 Excel 备份；误清数据后可用“导入 Excel 台账”恢复。

### 8.2 部署（Vercel 等静态托管）

- 适合 Vercel / Netlify / Cloudflare Pages 静态部署，网页托管很稳。
- 但**静态托管不存业务数据**，数据仍在各自浏览器里，部署后并不会变成云端共享。
- 注意：Hero 的 3D 机器人由 Spline 官方 CDN 加载，断网/内网环境会加载不出（不影响其它功能）。

### 8.3 多人共享 / 长期可靠 → 接数据库

需要多人共用同一份数据时，接入云数据库，替换 `src/lib/storage.js` 的读写实现即可，页面基本不动：

```text
Supabase          免费额度大、配置简单（国内访问偶尔不稳）
腾讯云开发 CloudBase 国内访问稳定，推荐国内场景
LeanCloud          国内备选
```

接入需改代码（可由 AI 完成），日常使用与后台数据管理（可视化表格）无需编程。

---

## 9. 后续可扩展

```text
图片上传（扫描前/后照片，需云存储）
权限管理（管理员 / 站点负责人 / 飞手 / 领导查看）
操作日志（谁建/改/完成/闭环）
Word 闭环单生成
领导统计看板（月度数量、各站点/线路、逾期率、闭环率）
```

---

## 10. 代码结构

```text
src/
  components/
    Layout            左侧导航（含“扫描任务”父子菜单）+ 顶栏 + 背景（懒加载）+ 全局光影 + 玻璃滤镜
    FloatingLines     背景流动线条（three.js，后台自动暂停）
    GlowCard          MagicBento 发光卡
    CursorSpotlight   全局鼠标光影（rAF 节流）
    LiquidGlass       液态玻璃组件（GlassSurface / GlassFilter）
    SplineScene /
    SplineHero        3D 机器人场景与首页 Hero
    Spotlight         聚光灯（SVG）
    BackButton        圆形玻璃返回按钮
    StatusBadge       状态标签
    ConfirmModal      玻璃风确认弹窗（统一替代原生 confirm）
  pages/
    Dashboard、TaskList、TaskForm、TaskDetail、DataExport
    （均按路由 React.lazy 代码分割；TaskList 覆盖 全部/复扫/通扫 三个子路由，按周分组展示，同周同线路合并母行可展开子行）
  lib/
    constants         状态（五态）、颜色、任务类型、逾期、时间工具
    storage           localStorage 读写 + 编号生成 + 旧数据迁移 + 导入合并（接数据库改这里）
    importExcel       Excel 台账解析导入（exceljs 动态加载，兼容旧版 13 列与新旧日期表头双名）
    sampleData        内置 5 条示例数据
    exportExcel       Excel 导出（exceljs 动态加载：周报 9 列美化 / 全部台账 16 列备份）
```
